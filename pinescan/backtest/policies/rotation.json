{
  "name": "rotation",
  "description": "Recycle capital into fresher setups: when full, sell the open position nearest its target (within a widening band) to fund a new entry.",
  "capital": {"total": 2000000, "max_concurrent": 10},
  "sizing": {"rule": "fixed_amount", "params": {"amount": 200000}},
  "selection": {"rule": "free_capital_first"},
  "rotation": {"rule": "nearest_to_target_band", "params": {"start": 10, "step": 10, "max": 40}},
  "exit": {"rule": "scanner_default"},
  "costs": {"brokerage_pct": 0.03, "slippage_pct": 0.05, "stt_pct": 0.025}
}
