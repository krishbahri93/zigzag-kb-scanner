# Secrets / scratch
.env
_probe.py
_*.py
# but _*.py must not swallow dunder package files
!__init__.py
!__main__.py
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# logs
*.log

# local Polygon API key (never commit)
.polygon_key

# generated cache + scan outputs (not committed)
data/
*.egg-info/

# live forward-test dashboards (regenerate every run; persisted state lives under data/)
reports/forward/

# local Dhan creds (never commit)
.dhan_creds
