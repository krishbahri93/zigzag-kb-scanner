name: ZigZag KB Scan
on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 3-10 * * 1-5"       # every 5 min during 8:30 AM - 3:30 PM IST
    - cron: "5 10,11 * * 1-5"        # one extra at 3:35 PM IST + one at 4:35 PM IST
permissions:
  contents: write
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install -q yfinance pandas numpy requests dhanhq
      - name: run scan
        env:
          DHAN_CLIENT_ID: ${{ secrets.DHAN_CLIENT_ID }}
          DHAN_ACCESS_TOKEN: ${{ secrets.DHAN_ACCESS_TOKEN }}
          KWM_DATA_SOURCE: dhan
        run: python run_scan.py
      - name: publish JSON feeds
        run: |
          git config user.name "zigzag-kb-bot"
          git config user.email "bot@users.noreply.github.com"
          git add results.json performance.json
          git commit -m "update results $(date -u +%H:%M)" || echo "no change"
          git push

